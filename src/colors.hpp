#ifndef GAME_COLORS
#define GAME_COLORS
#include <SFML/Graphics.hpp>

const sf::Color RAILS_COLOR = sf::Color(64, 32, 32);
const sf::Color CLOTH_COLOR = sf::Color::Green;
const sf::Color POCKET_COLOR = sf::Color::Black;
const sf::Color CUE_BALL_COLOR = sf::Color::White;
const sf::Color BALL_COLOR = sf::Color::Red;

#endif